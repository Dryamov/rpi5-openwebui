# ##############################################################################
# OPEN WEBUI - COMPREHENSIVE CONFIGURATION EXAMPLE
# ##############################################################################
# This file contains a list of important environment variables for Open WebUI.
# Note: Variables marked as 'PersistentConfig' in the code are stored in the 
# database after first launch. To force reading from environment variables, 
# set ENABLE_PERSISTENT_CONFIG=False.
# ##############################################################################

# --- BASIC SETTINGS ---
WEBUI_NAME=Open WebUI
# WEBUI_URL=http://localhost:8080
# WEBUI_SECRET_KEY=  # GENERATE THIS! (e.g. openssl rand -hex 32)
# ENV=prod  # Set to 'dev' to enable Swagger API docs

# --- USER REGISTRATION & ROLES ---
ENABLE_SIGNUP=true
DEFAULT_USER_ROLE=pending
ENABLE_LOGIN_FORM=true
# ADMIN_EMAIL=admin@example.com

# --- OLLAMA SETTINGS ---
ENABLE_OLLAMA_API=true
OLLAMA_BASE_URL=http://ollama:11434
# OLLAMA_BASE_URLS=http://ollama1:11434;http://ollama2:11434

# --- OPENAI & EXTERNAL APIS ---
ENABLE_OPENAI_API=true
# OPENAI_API_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=sk-xxxx
# OPENAI_API_CONFIGS={"url": "https://api.openai.com/v1", "key": "sk-xxxx"}

# --- RAG (Retrieval Augmented Generation) & SEARCH ---
# RAG_EMBEDDING_ENGINE=ollama  # Options: '', 'ollama', 'openai', 'webui' (local)
# RAG_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# RAG_TOP_K=3
# RAG_RELEVANCE_THRESHOLD=0.0
# ENABLE_RAG_HYBRID_SEARCH=false
# RAG_HYBRID_BM25_WEIGHT=0.5

# --- WEB SEARCH (via SearXNG) ---
# ENABLE_SEARCH_ENGINE=true
# SEARXNG_QUERY_URL=http://searxng:8080/search?q=<query>
# RAG_WEB_SEARCH_ENGINE=searxng
# RAG_WEB_SEARCH_RESULT_COUNT=3

# --- OAUTH / SSO SETTINGS ---
# ENABLE_OAUTH_SIGNUP=false
# OAUTH_MERGE_ACCOUNTS_BY_EMAIL=false

# Google Example:
# GOOGLE_CLIENT_ID=xxxx
# GOOGLE_CLIENT_SECRET=xxxx
# GOOGLE_REDIRECT_URI=https://ai.example.com/oauth/google/callback

# --- SECURITY & CORS ---
ENABLE_CORS=false
CORS_ALLOW_ORIGIN=*

# --- DATABASE & STORAGE ---
# DATABASE_URL=sqlite:///./data/webui.db
# STORAGE_PROVIDER=local  # Options: local, s3
# DATA_DIR=/app/backend/data

# --- ADVANCED ---
# JWT_EXPIRES_IN=4w
# ENABLE_API_KEYS=false
# SHOW_ADMIN_DETAILS=true
# HF_HUB_OFFLINE=0  # Set to 1 for completely offline environments
