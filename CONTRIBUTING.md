# CONTRIBUTING.md

## Добро пожаловать!

Спасибо за интерес к проекту `rpi5-openwebui`! Это руководство поможет вам внести свой вклад в развитие проекта.

## 📋 Оглавление

- [Добавление новых сервисов](#добавление-новых-сервисов)
- [Обновление скриптов backup/restore](#обновление-скриптов-backuprestore)
- [Code Style](#code-style)
- [Тестирование](#тестирование)
- [Процесс Pull Request](#процесс-pull-request)

## Добавление новых сервисов

### 1. Обновление `docker-compose.yml`

При добавлении нового сервиса следуйте этим правилам:

#### Обязательные поля

```yaml
services:
  новый-сервис:
    image: ${IMAGE_VAR:-default-image:tag}
    container_name: service-name
    restart: unless-stopped
    networks:
      - backend-net
    
    # Health check (обязателен для критичных сервисов)
    healthcheck:
      test: ["CMD", "проверка-команда"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    
    # Используйте YAML anchor для логирования
    logging: *default-logging
    
    # Лимиты ресурсов (ОБЯЗАТЕЛЬНО для RPi5!)
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
```

#### Именованные volumes

Если сервис использует volume:

```yaml
volumes:
  - service_data:/path/to/data

# В секции volumes:
volumes:
  service_data:
    name: service_data
    labels:
      com.backup: "true"  # Включить в бэкап
      com.service: "service-name"
```

### 2. Обновление backup/restore

#### Добавление нового volume в бэкап

Отредактируйте `scripts/backup.config`:

```bash
BACKUP_VOLUMES=(
    # ... существующие volumes ...
    "service_data"  # Новый volume
)
```

#### Исключение тяжелых данных

Если в volume есть кэш или временные файлы:

```bash
VOLUME_EXCLUDES=(
    # ... существующие исключения ...
    "service_data:cache"      # Исключить директорию cache
    "service_data:tmp/*.log"  # Исключить логи
)
```

#### Добавление bind mounts

Для локальных директорий (bind mounts):

```bash
BACKUP_FILES=(
    # ... существующие файлы ...
    "новый-сервис/config"  # Путь относительно корня проекта
)
```

### 3. Документация

Создайте файл `docs/services/новый-сервис.md`:

```markdown
# Конфигурация [Название Сервиса]

## Обзор
Краткое описание сервиса и его роли в стеке.

## Конфигурационные файлы
Список конфигов и volumes.

## Переменные окружения
Таблица с обязательными и опциональными переменными.

## Troubleshooting
Частые проблемы и их решения.
```

### 4. Обновление README.md

Добавьте новый сервис в архитектурную диаграмму Mermaid и секцию Setup Instructions.

## Обновление скриптов backup/restore

### Правила разработки

1. **Логирование**: Все операции должны логироваться через функции `log_info`, `log_warn`, `log_error`
2. **Проверки**: Добавляйте валидацию входных данных
3. **Trap handlers**: Используйте `trap cleanup EXIT` для гарантии перезапуска контейнеров
4. **Версионирование**: Указывайте версию скрипта в комментарии `# V2.X`

### Пример добавления новой функции

```bash
# Новая функция для проверки
validate_something() {
    local param=$1
    
    if [ -z "$param" ]; then
        log_error "Параметр не может быть пустым"
        return 1
    fi
    
    log_info "Валидация успешна: $param"
    return 0
}
```

## Code Style

### Shell Scripts

```bash
# Используйте 4 пробела для отступов (см. .editorconfig)
if [ condition ]; then
    echo "правильный отступ"
fi

# Всегда кавычите переменные
echo "$VAR" || log_error "Ошибка: $VAR"

# Используйте функции для повторяющегося кода
my_function() {
    local arg1=$1
    echo "Processing: $arg1"
}
```

### YAML (docker-compose.yml, configs)

```yaml
# 2 пробела для отступов
services:
  example:
    image: example:latest
    environment:
      - KEY=value  # Без кавычек внутри значений!
```

### Markdown

- Используйте GitHub Flavored Markdown
- Добавляйте примеры кода для сложных инструкций
- Используйте alerts (`> [!WARNING]`) для важной информации

## Тестирование

### CI/CD Requirements

Проект использует GitHub Actions для автоматического тестирования. При каждом push и pull request запускаются следующие проверки:

- **Docker Compose Validation** — проверка синтаксиса и конфигурации
- **ShellCheck** — валидация bash скриптов
- **Backup Test** — тестирование скриптов backup/restore (еженедельно)

> [!IMPORTANT]
> Все pull requests должны проходить CI/CD проверки перед merge. Если тесты не проходят, PR не будет одобрен.

### Локальное тестирование перед коммитом

#### 1. Docker Compose валидация

```bash
# Синтаксис и конфигурация
docker compose config

# YAML линтинг
yamllint -c .yamllint.yml docker-compose.yml
```

Не должно быть ошибок парсинга.

#### 2. Shell Scripts проверка

```bash
# Установить shellcheck (если еще не установлен)
sudo apt-get install shellcheck

# Проверить все скрипты
find scripts -name "*.sh" -exec shellcheck {} \;
```

Исправьте все ошибки (errors), warnings допустимы.

#### 3. Валидация переменных окружения

```bash
# Проверить, что все переменные из docker-compose.yml определены
./scripts/ci/validate-env-vars.sh
```

#### 4. Проверка Health Checks

```bash
# Проверить наличие healthcheck для критичных сервисов
./scripts/ci/validate-healthchecks.sh
```

#### 5. Health checks работоспособности

```bash
docker compose up -d
sleep 60  # Дайте время на инициализацию
docker compose ps

# Все сервисы должны быть healthy
```

#### 6. Backup/Restore

```bash
# Создать тестовый бэкап
./scripts/backup.sh

# Проверить содержимое
tar -tzf backups/последний_бэкап.tar.gz | head -20

# Тест восстановления (ОСТОРОЖНО: перезапишет данные!)
# Делайте это только в тестовом окружении!
./scripts/restore.sh backups/последний_бэкап.tar.gz
```

### Установка инструментов для локального тестирования

```bash
# ShellCheck для проверки bash скриптов
sudo apt-get install shellcheck

# yamllint для проверки YAML файлов
pip install yamllint

# Docker Compose (если еще не установлен)
# см. официальную документацию Docker
```

## Процесс Pull Request

### 1. Fork и клонирование

```bash
git clone https://github.com/ваш-username/rpi5-openwebui.git
cd rpi5-openwebui
```

### 2. Создание feature branch

```bash
git checkout -b feature/описание-изменения
```

### 3. Внесение изменений

- Следуйте Code Style
- Добавьте документацию
- Обновите CHANGELOG.md

### 4. Коммит

```bash
git add .
git commit -m "feat: краткое описание изменения

Подробное описание того, что было изменено и почему.

Closes #123"  # Если PR закрывает issue
```

#### Формат коммитов (Conventional Commits)

- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `refactor:` — рефакторинг кода
- `test:` — добавление тестов
- `chore:` — обновление зависимостей, конфигов

### 5. Push и создание PR

```bash
git push origin feature/описание-изменения
```

Создайте Pull Request на GitHub с описанием:
- Что изменено
- Зачем это нужно
- Как тестировалось

### 6. Code Review

Ваш PR будет проверен. Будьте готовы внести правки по feedback.

## Вопросы?

Если у вас есть вопросы:
1. Проверьте [docs/](file:///home/dryamov/Repositories/rpi5-openwebui/docs/) для документации
2. Создайте [GitHub Issue](https://github.com/ваш-username/rpi5-openwebui/issues)
3. Опишите проблему максимально детально

---

Спасибо за ваш вклад! 🎉
